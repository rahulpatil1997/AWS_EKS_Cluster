apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-security-groups: "sg-03e4abaaea1156715"
spec:
  selector:
    app: frontend
    app.kubernetes.io/instance: frontend
    app.kubernetes.io/name: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000  
  type: LoadBalancer

